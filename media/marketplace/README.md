# Marketplace Assets

This directory contains marketplace-facing images used by `README.md` and VS Code extension listing metadata.

## Required Assets

- `../icon.png` (`128x128` PNG) - extension icon used by `package.json` (`icon` field)
- `./banner.png` (recommended `1280x640`) - listing/banner image
- `./screenshots/*.gif` - screenshot set for key workflows
- `./images/*.png` / `*.gif` - feature highlight media

## Current State

The current files are placeholders to keep release wiring complete and unblock packaging/publishing automation. Replace with real product visuals before final marketplace launch.

## Suggested Screenshot Set

1. Run SQL and results panel
2. ERD generation output
3. Connection setup/introspection flow

